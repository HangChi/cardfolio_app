# MEMORY.md - 项目长期记忆

_按主题整理。从每日日志里提炼出仍然成立的事实沉淀到这里。_

使用方式：
- 每个 `## <主题>` 是一个长期稳定的事实分类。
- 条目简短、陈述式；如果在描述一个决策，最好带上日期。
- 事实变化时**直接修改**对应条目（只保留最新状态）。
- 批量蒸馏用 `distill_memory.py`；需要润色时手动整理。

---

## 工程基线

- 2026-08-09：数据库当前模式版本为 v9，模式升级必须保留每个版本快照并验证 1..N 的分步迁移。
- 2026-08-09：本地与 CI 的质量门禁为格式、严格静态分析、迁移专项、串行全量测试和 Android debug 构建。

## 项目约定

- 数据库模式变更必须新增 `drift_schemas/app/` 快照并重新生成 `test/drift/app/generated/` 迁移夹具，不能修改旧快照来代表新版本。
- 新卡片名称和城市必填；月度新增按获得日期；软删除目标不计入有效总额但保留审计记录。
- 当前事实源优先使用 README、业务规则、架构、当前 Feature README、质量门禁和当前代码；
  历史计划与旧测试数量保留，但必须标明日期和历史证据属性。
- OCR 平台桥接和候选确认 UI 已存在，但当前运行时不可用；完成诊断、回归和真机验收前
  不得标记为已交付能力。

## 决策记录

- 2026-08-09：v9 的卡组封面字段作为 Drift 类型化列维护，避免创建路径与迁移路径产生模式漂移。
- 2026-08-09：CI 固定 Flutter 3.44.0 与 Java 21，并显式运行迁移专项和串行全量测试。

## 用户偏好

_记用户明确说过希望被怎么协助。_
