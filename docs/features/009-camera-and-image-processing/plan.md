# Feature 009 实施计划

> 本计划在当前会话内联执行；禁止创建分支、禁止子智能体、禁止启动模拟器；
> 所有实现完成后只提交一次。

## 技术栈与全局约束

Flutter、Riverpod、go_router、Drift、`image_picker` 1.2.3、`image` 4.9.1。
生产行为先写失败测试并确认正确失败。规格的范围、上限、处理顺序、文件生命周期
和设备验证边界对每项任务生效。

## 任务 1：领域与处理管线

**文件：** 新增 `lib/features/cards/domain/image_processing.dart`、
`lib/features/cards/data/image_processing/local_image_processor.dart` 及对应测试。

1. 先写坐标、四角、参数、自动边缘、处理输出和错误测试并运行到预期失败。
2. 实现不可变值对象、处理契约、纯 Dart 解码/处理/编码和 isolate 调度。
3. 运行目标测试到通过并消除重复。

## 任务 2：相机与连续草稿

**文件：** 新增相机契约/适配器；修改卡片 provider、建卡状态和控制器及测试。

1. 先写相机成功、取消、权限、恢复、连续拍摄和 20 张上限测试并运行到预期失败。
2. 实现 `image_picker` 适配器和控制器状态转换。
3. 运行目标测试到通过。

## 任务 3：原图/派生图持久化

**文件：** 修改图片模型、受管存储、仓储和数据库操作及测试。

1. 先写派生 JPEG 导入、原图字节不变、数据库关联和失败补偿测试并运行到预期失败。
2. 实现 `derived/` 存储、请求字段和仓储双文件协调。
3. 运行目标测试与既有图片回归测试。

## 任务 4：拍摄入口与编辑 UI

**文件：** 修改路由、拍摄入口、新建卡片页；新增编辑控制器/页面及 Widget 测试。

1. 先写入口可用性、连续模式、编辑控件、撤销/重置、处理成功/失败测试。
2. 接线单张/连续拍摄、草稿预览和编辑结果。
3. 运行目标 Widget 测试。

## 任务 5：平台、文档与全量验证

1. 添加 iOS 相机/相册用途声明，更新架构、追踪、开发日志和 Feature 状态。
2. 格式化变更文件。
3. 运行 Feature 目标测试、全量 `flutter test` 和 `flutter analyze`。
4. 检查 diff、敏感路径、未跟踪文件和模拟器手测步骤。
5. 在当前分支创建一次提交。
