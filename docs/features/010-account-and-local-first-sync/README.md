# Feature 010：账号与本地优先同步

## 目标

用户可在不牺牲本地离线能力的前提下登录并选择同步；离线变更最终上传，重复重试不重复，无法自动合并的修改生成冲突副本。

## 范围

- 账号注册/登录/退出和本地保留选择；
- 同步开关、队列、状态、重试；
- 多设备增量同步、删除墓碑和冲突副本；
- 数据下载、账号与云端数据删除；
- 本地数据始终可离线浏览和编辑。

## 追踪

- FR-ACC-001..002、FR-SYNC-001..003；
- BR-SYNC-001..004；
- NFR-SYNC-001、NFR-DATA-001、SEC-001..004；
- AC-V1-001..003。

## 架构边界

本地数据库仍是客户端读取源。每个实体使用 UUID、UTC 时间、服务端版本和删除语义；
每次操作有稳定幂等 ID。[ADR-009](../../architecture/adr/ADR-009-account-sync-service-and-protocol.md)
选择 Supabase 作为首个服务端实现，并以可替换 REST v1 隔离客户端。

## 证据门禁

- [x] ADR-009 比较 Supabase/Firebase 的离线、冲突、附件、费用、地区、删除和锁定；
- [x] REST v1、Supabase RLS/私有对象/幂等函数和威胁模型已定义；
- [x] 20 条离线重放、同字段冲突、退出保留本地三项主机原型已通过；
- [x] 数据处理清单和账号删除保留/清理流程已完成；
- [ ] 隐私政策运营主体、地区、DPA、未成年人和保留期限字段待发布前确认；
- [x] 响应丢失重放、时钟偏差和附件大小/校验契约的宿主机基线已建立；
- [x] 网关直写权限、并发幂等、删除门禁和上游超时已实现静态/单元守卫；
- [ ] 真实 Supabase 并发、接近 64 MiB 附件中断和双设备故障注入待环境验收。

## 风险

静默覆盖、重试重复、删除复活、权限越界、令牌泄露、供应商锁定和费用失控。

## 交付状态

客户端、schema v9、16 类同步实体、图片附件、账号/同步 UI、Node REST 网关、Supabase
迁移和九件套已完成。宿主机自动化与静态分析证据见 [测试矩阵](test-matrix.md)。生产发布仍需部署
REST 网关、配置实际 Supabase 项目、完成运营主体/DPA/地区/费用专项评审，并按测试
矩阵执行双设备手动验收；未配置 `CARD_FOLIO_API_BASE_URL` 时不会上传任何用户内容。

历史首次交付验证：Feature 010 定向宿主机测试 88 项、当时全量宿主机测试 377 项、
静态分析 0 问题；未启动模拟器或真机，也未部署远端服务。当前项目验证见
[工程基线](../../engineering-baseline.md)。
