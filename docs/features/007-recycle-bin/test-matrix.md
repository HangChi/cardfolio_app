# Feature 007 测试矩阵

| ID | 层级 | 覆盖 | 自动化 |
|---|---|---|---|
| T01 | Domain | 保留期、剩余天数、影响模型 | 是 |
| T02 | Query | 软删除后普通查询排除、回收站包含 | 是 |
| T03 | Query | 恢复后同一查询口径和图片关联回归 | 是 |
| T04 | Query | 统计与套卡进度响应式重算 | 是 |
| T05 | Query | 永久删除依赖顺序、影响数量与定义保留 | 是 |
| T06 | Queue | 文件路径去重、成功出队、失败留队 | 是 |
| T07 | Retention | 7/30/90 设置、边界到期与启动清理 | 是 |
| T08 | Migration | v5 → v6 数据保留与新表创建 | 是 |
| T09 | Repository | 幂等操作与安全失败映射 | 是 |
| T10 | Widget | 详情删除、回收站空/成功/错误与二次确认 | 是 |
| T11 | App | `/recycle-bin` 路由与“我的”入口 | 是 |
| T12 | Device | 返回、对话框、SnackBar 与重启恢复 | 手动，待执行 |
| T13 | Device | 360/393/430 宽度和 200% 字体 | 手动，待执行 |

## 手动设备步骤（未执行）

1. 启动 Android 模拟器或连接真机，运行 `flutter run -d <device-id>`。
2. 新建一张含两张图片、标签、套卡成员和购买记录的卡片。
3. 从详情页删除，核对收藏、首页、统计、套卡进度和购买目标立即排除该卡片。
4. 从“我的 → 回收站”恢复，核对图片、组织资料、购买快照和套卡进度原样回归。
5. 再次删除，打开永久删除确认，核对图片数、文件数和购买关联数。
6. 确认永久删除并重启应用，核对条目不再出现且其他购买目标与套卡结构仍在。
7. 分别选择 7、30、90 天并重启，核对设置持久化。
8. 分别在 360、393、430 逻辑像素宽度与系统字体 200% 下检查列表、下拉框和对话框无溢出。

## 自动化结果

- `flutter test test\features\recycle_bin`：17 项通过。
- `flutter test test\drift\app\migration_test.dart test\app\cardfolio_app_test.dart`：26 项通过。
- `flutter test`：272 项通过。
- `flutter analyze`：0 个问题。

T01..T11 已通过；T12..T13 未执行并保持待验收。整个自动化过程未启动模拟器或真机。
